apiVersion: v1
kind: Pod
metadata:
  name: nginx-label
  labels:
    team: IT
    version: 1.1.1
    environment: production
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80